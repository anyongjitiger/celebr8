enum Table {
  File = 'file',
  Member = 'members',
  Experience = 'experience',
  ExpeType = 'expe_type',
  ExpeTypeMusic = 'expe_type_music',
  Invite = 'invite',
  TEST = 'test'
}

enum Procedure { }

enum ActivityType {
  Party = 1,
  Birthday,
  Meeting,
  Trip,
  Wedding,
  Others,
}

export { Table, Procedure, ActivityType };
